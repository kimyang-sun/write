const exporess = require('express');

const { Post } = require('../../models');

// const router = express.Router();

// router.get('/', async (req, res, next) => {
//   try {
//     const posts = await Post.findAll({
//       where: { Limit: 5, offset: 0 },
//     });
//     res.status(200).json(posts);
//   } catch (e) {
//     console.error(e);
//     next(e);
//   }
// });
